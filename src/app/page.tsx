import { Layout } from "@/components/widgets/layout";

export const metadata = {
  title: 'Fably',
  description: 'Fably',
}

export default function Page() {
  return (
    <Layout>
      &nbsp;
    </Layout>
  )
}