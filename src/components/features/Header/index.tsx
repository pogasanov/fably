import { Login } from "@/components/features/Login";
import { Container } from "@/components/shared/ui";

export const Header = () => {
  return (
    <Container>
      <Login/>
    </Container>
  )
}